{
  "name": "spin-protocol-dapp",
  "version": "1.0.0",
  "description": "SPIN Protocol Decentralized Backend",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "STAGE=prod truffle migrate --reset --network cypress",
    "dev": "STAGE=dev truffle migrate --reset --network baobab",
    "staging": "STAGE=staging truffle migrate --reset --network baobab",
    "update-prod": "STAGE=prod truffle migrate --reset --network cypress --f 3",
    "update-dev": "STAGE=dev truffle migrate --reset --network baobab --f 3",
    "update-staging": "STAGE=staging truffle migrate --reset --network baobab --f 3"
  },
  "author": "Harace Moon",
  "license": "ISC",
  "devDependencies": {
    "caver-js": "^1.0.0",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "chai-bignumber": "^3.0.0",
    "chai-shallow-deep-equal": "^1.4.6",
    "eslint": "^5.16.0",
    "eth-gas-reporter": "^0.1.12",
    "ffp-js": "^0.0.19",
    "solhint": "^2.0.0"
  },
  "dependencies": {
    "moment": "^2.24.0",
    "moment-timezone": "^0.5.26",
    "truffle-hdwallet-provider-klaytn": "^1.0.13-a"
  }
}
